EVAL "local artists = redis.call('smembers', 'test:artists') local albums = redis.call('smembers', 'test:albums') local songs = redis.call('smembers', 'test:songs') redis.call('del', 'test:artists:nextid', 'test:artists', 'test:albums:nextid', 'test:albums', 'test:songs:nextid', 'test:songs', 'test:relationships') for k,v in pairs(artists) do redis.call('del', 'test:artists:' .. v) end for k,v in pairs(albums) do redis.call('del', 'test:albums:' .. v) end for k,v in pairs(songs) do redis.call('del', 'test:songs:' .. v) end return redis.status_reply('done')" 0